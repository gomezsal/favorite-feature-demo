generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

// Model for storing favorite players
model item {
  id   String @id @map("_id")
  data Json
  
  @@map("items")
}